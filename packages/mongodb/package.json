{
  "name": "mongodb",
  "version": "1.0.0",
  "description": "local mongodb",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:mongo": "docker-compose -f compose.yml up",
    "start:mongo-ui": "open-cli http://localhost:8081",
    "start:dev": "run-p start:mongo start:mongo-ui"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "open-cli": "^6.0.1"
  }
}
